# 逻辑辩论文字游戏（Godot 4 · 手机端 · 纯离线 · 聊天气泡界面）

一个**完全离线、不联网、不接任何云端 AI** 的文字博弈游戏。用户输入自己的观点/论证，一个“离线 AI”（本地规则引擎）用逻辑谬误与论证结构来“杠”你。界面是**类似聊天气泡的风格**，简洁清爽。

## 它是怎么“离线 AI”的

它**不是**真正的大模型——因为你要完全不接 AI、不联网。所以这里用的是**本地规则引擎**：

- `KnowledgeBase.gd` 相当于“大脑”，存放了《逻辑与辩论资料库》里的**20+ 种谬误规则**、苏格拉底式追问、归谬/通用攻击模板、辩题库、**真实争议议题知识库**（含各议题的立场锚点/核心冲突/事实弹药）、**经典辩论赛案例库**（人性本善/金钱辩等，含出处与可复用攻防技法）、经典辩手句式（`DEBATE_MOVES`）与结算语。
- `DebateEngine.gd` 负责“理解”：用关键词 + 正则**侦测**你输入里命中哪种逻辑谬误，命中就给出**「识别 → 拆解 → 反问 → 归谬」**四连击；未命中则切换为**苏格拉底追问 / 通用拆解 / 归谬**。
- 带**计分、回合、辩题、结算判定**系统。

所以你“抛出一个观点”，它“用逻辑漏洞来杠你”。这套数据完全取自项目根目录的 `逻辑与辩论资料库/`。

## 功能一览

| 功能 | 说明 |
|---|---|
| 🎨 聊天气泡界面 | 玩家气泡靠右（绿）、AI 气泡靠左（深灰），含头像、昵称、时间戳；自动滚到底 |
| 🧠 20+ 种谬误识别 | 诉诸大众/权威、假两难、人身攻击（含处境）、滑坡、诉诸情感/无知/传统/自然/后果、红鲱鱼、偷换论题、循环论证、合成/分解、虚假类比、假因果、复杂问句、幸存者偏差、完美主义、中间立场等 |
| 🎯 随机辩题 | AI 主动抛出一个辩题（从辩题库随机抽），点「换辩题」即可重抽；含 **真实争议议题**（死刑/安乐死/UBI/AI意识等）与 **经典国际大专辩论赛辩题**（人性本善/金钱辩/以成败论英雄等，附出处与技战法） |
| 🏁 回合与胜负结算 | 顶部显示双方计分条 + 回合数；回合结束自动弹「结算面板」，给出 命中/讲理/胜负判定 |
| 😜 Emoji 嘲讽 | AI 自动按场景调用 emoji：命中你的谬误（🤡💀🙃）、称赞你讲理（👏🎉🧠）、追问/归谬/开场/结算/安抚各有专属 emoji；按概率触发，不会太吵 |
| 🧭 情绪化应对 | 检测到人身攻击/脏话/极短语/发泄词（“蠢货”“凭什么”“死”）时不硬杠，改先安抚+教学，引导回论点；记为“说错”，拉低结算 |
| 🎓 求助（见招拆招） | 卡住时点「求助」，AI 教你一条判定原则 + 一种反杀技巧 |
| 🕊️ 让步 / 标准答案 | 命中谬误后 AI 偶尔给出“你若能举证我也认账”的让步，避免无限杠 |
| 🔒 防刷屏 | 发送冷却 + 短文本/重复文本拦截，防止垃圾信息打乱节奏 |
| 🧾 计分明细 | 顶部实时显示“你讲理 X / AI 命中 Y”；结算面板说明判定标准与取胜要点 |
| 三档难度 | 温和 / 毒舌 / 归谬狂魔（越高越爱用归谬术） |
| 💾 导出对话 | 把整段对话导出为 `user://辩论记录_*.md` 文件 |
| 🔄 重开 | 清空重来 |

## 各文件

| 文件 | 作用 |
|---|---|
| `project.godot` | 项目配置（手机渲染、竖屏） |
| `scenes/Main.tscn` | 主场景（挂 Main.gd） |
| `scripts/Main.gd` | **界面**：聊天气泡、计分、辩题、结算面板、导出 |
| `scripts/DebateEngine.gd` | **引擎**：侦测谬误 → 四连击 / 兜底；回合与结算 |
| `scripts/KnowledgeBase.gd` | **数据**：全部谬误规则、追问、归谬、辩题库、结算语 |

## 怎么运行

### 直接导入（最快，一版即跑）
在仓库根目录已打包好 **`godot_logic_game_import.zip`**（约 28KB，无需分卷）。解压后用 **Godot 4.7.1 / 4.7.2 stable** 打开 `godot_logic_game/project.godot`，点 ▶ 即刻可玩。

### 桌面预览
1. 用 **Godot 4.7.1 stable** 打开本文件夹（`godot_logic_game/project.godot`）。
2. 点 ▶ 运行即可在窗口中玩。

### 手机端（Android）
1. Godot 4.7.2 → 菜单 → 安装 **Android 构建模板（Build Template）**（首次）。
2. `项目 → 导出 → 添加 → Android`。
3. 配置 **Keystore / 签名 / 包名**（首次建一个 debug 密钥）。
4. 点「导出项目」得 `.apk`，安装到手机扫码可用；输入框会自动唤起软键盘。

### 手机端（iOS）
- 需 macOS + Xcode，经 Godot 导出后再打包。逻辑完全一致。

> 真机运行时**可完全断网**：整个游戏不发起任何网络请求、不调用任何外部 AI——所有“反驳”都来自本地的 `KnowledgeBase.gd`。

## 接招逻辑

- 你说的话 → **先扫情绪**（人身攻击/脏话/极短语/发泄词）：有就“安抚+教学”，不硬杠，记为说错。
- 再扫谬误 → 有就“命中”，给你四连击（识别/拆解/反问/归谬），偶尔附一句“让步/标准答案”。
- 你说得像在讲理（含“证据 / 因为 / 所以 / 反例 / 比如 / 假设…”）→ 引擎会夸你并给玩家加分。
- 你提问 → 苏格拉底式追问。
- 其余 → 通用拆解 / 归谬。
- 卡住 → 点「求助」，AI 教你一条判定原则 + 一种反杀技巧。

## 怎么拓展（把《资料库》更多内容加进去）

想让它“懂得更多”，**只改 `scripts/KnowledgeBase.gd`、不改引擎**：

- 在 `FALLACIES` 数组加一条 `{ "name", "en", "signals", "regex", "explain", "probe", "reductio" }`，即可多识别一种谬误。
- 在 `SOCRATIC / ATTACKS / REDUCTIO / PRAISE` 里加句子，让接招更丰富。
- 在 `TOPICS` 里加 `{ "topic", "ask" }`，增加更多辩题。
- 想接入真实本地 LLM（如 Ollama/llama.cpp），把 `DebateEngine.respond()` 换成调用本地推理即可——但那属于另一个“接 AI”的方向。

## 常见问题（排障）

**现象：点发送后，玩家/AI 气泡只剩昵称、内容空白，或 AI 一连串空气泡。**
这类问题几乎都来自**旧缓存**或**导入夹层**，并非逻辑缺陷。请按顺序排查：

1. **彻底清掉旧项目**：在手机上删除旧的 `godot_logic_game` 目录，同时删除项目里的 `.godot/` 缓存目录（Godot 会缓存旧编译脚本）。
2. **重新解压最新 ZIP 到全新空目录**，不要覆盖旧目录：
   - 下载：`https://github.com/BJX-lin/-md-/raw/arena/01a067bf-md/godot_logic_game_import.zip`
   - 用 Godot **4.7.2 stable**（Mobile Editor）打开 `godot_logic_game/project.godot`。
3. 若仍异常，点顶部「重开」确保引擎复位；检查是否命中「导出/结算」等正常弹层。

**说明**：当前版本气泡已改为 Godot 4 可靠布局（宽度上限在 `PanelContainer`，正文用 `RichTextLabel` 自动换行 + `fit_content`），且引擎即使意外返回空也会兜底给一句回应，**不会再出现“只有名字、内容空白”的空气泡**。若解压后 `scripts/Main.gd` 里搜不到 `scroll_active`，说明抓到的是旧版，请重新下载最新 ZIP。

## 说明

本工程与根目录 `逻辑与辩论资料库/` 配套，语料取自该库。纯本地、零联网、零云端 AI。
